import { StartButton } from "./StartButton";

export function Taskbar() {
  return (
    <div id="taskbar" className="noselect">
      <StartButton /> TASK BARTASK BARTASK BARTASK BARTASK BARTASK BAR
    </div>
  );
}
